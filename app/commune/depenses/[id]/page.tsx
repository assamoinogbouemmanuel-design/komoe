"use client";

import TransactionDetailPage from "@/app/commune/transactions/[id]/page";

// La page de détail d'une dépense est identique à celle d'une transaction dans la structure
export default function DepenseDetailPage() {
  return <TransactionDetailPage />;
}
